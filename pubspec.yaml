name: my_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.8.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # SDK для MyID, последняя доступная на август 2025
  myid: ^1.3.9

  # Поддержка локализации
  flutter_localizations:
    sdk: flutter

  # Иконки в iOS стиле
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  # Последняя версия набора линтов
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
